#include<iostream>
#include<algorithm>
#include<cmath>
#define intt long long
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    intt t;
    cin >> t;
    while(t--)
    {
        intt l,a,b,max;
        cin>>l>>a>>b;
        intt give=a;
        max=a;
        while(1)
            {
                give;+
            }
    }

    return 0;
}